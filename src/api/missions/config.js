const closestUrl = '/find-closest';
const isolationUrl = '/countries-by-isolation';
const Provider = 'Missions';
const dbModel = 'Mission';
// const selector = 'missions';

export {
    isolationUrl,
    closestUrl,
    Provider,
    dbModel,
    // selector
};
